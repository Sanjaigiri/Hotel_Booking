// Optional: toggle password visibility (simple version)
document.addEventListener("DOMContentLoaded", function() {
  const passwordInput = document.getElementById("password");

  // Check if we want to toggle password, you can add a toggle button if needed
  // For minimal, no extra JS is needed
});
